- if object.errors.any?
  .error_explanation
    .alert.alert-error
      = "The form contains #{object.errors.count} #{'error'.pluralize(object.errors.count)}"
      %ul
        - object.errors.full_messages.each do |message|
          %li #{message}